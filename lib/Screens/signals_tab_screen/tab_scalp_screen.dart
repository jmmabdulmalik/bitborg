import 'package:flutter/material.dart';

class TabScalpScreen extends StatelessWidget {
  const TabScalpScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}