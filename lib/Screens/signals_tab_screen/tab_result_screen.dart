import 'package:flutter/material.dart';

class TabResulTScreen extends StatelessWidget {
  const TabResulTScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}