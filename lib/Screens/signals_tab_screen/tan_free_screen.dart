import 'package:flutter/material.dart';

class TabFreeScreen extends StatelessWidget {
  const TabFreeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}