import 'package:flutter/material.dart';


class PapularEvent extends StatelessWidget {
  const PapularEvent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
